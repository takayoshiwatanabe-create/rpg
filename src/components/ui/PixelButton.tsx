```diff
--- a/src/components/ui/PixelButton.tsx
+++ b/src/components/ui/PixelButton.tsx
@@ -128,16 +128,16 @@
   },
   // Text Variants
   text_primary: {
-    color: COLORS.cream,
+    color: COLORS.textDefault,
   },
   text_secondary: {
-    color: COLORS.cream,
+    color: COLORS.textDefault,
   },
   text_danger: {
-    color: COLORS.cream,
+    color: COLORS.textDefault,
   },
   text_ghost: {
-    color: COLORS.cream,
+    color: COLORS.textDefault,
   },
   textDisabled: {
     color: COLORS.gray,
```
